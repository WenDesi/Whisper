<Project>
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <PropertyGroup Label="Module paths">
    <SrcRoot>$(MSBuildThisFileDirectory)src\</SrcRoot>
    <CoreRoot>$(SrcRoot)core\</CoreRoot>
    <UiRoot>$(SrcRoot)ui\</UiRoot>
    <SttRoot>$(SrcRoot)stt\</SttRoot>
    <LlmRoot>$(SrcRoot)llm\</LlmRoot>
    <TranscriptRoot>$(SrcRoot)transcript\</TranscriptRoot>
    <ServerRoot>$(SrcRoot)server\</ServerRoot>
    <ProtoRoot>$(SrcRoot)proto\</ProtoRoot>
    <JobsRoot>$(SrcRoot)jobs\</JobsRoot>
    <SharedRoot>$(SrcRoot)shared\</SharedRoot>
  </PropertyGroup>

  <PropertyGroup>
    <!-- Redirect build artifacts to repo-level build/ directory.
         Required for WPF projects in flattened directories (src/, tests/)
         to prevent temp project from scanning obj/ generated files. -->
    <BaseIntermediateOutputPath>$(MSBuildThisFileDirectory)build\obj\$(MSBuildProjectName)\</BaseIntermediateOutputPath>
    <BaseOutputPath>$(MSBuildThisFileDirectory)build\bin\$(MSBuildProjectName)\</BaseOutputPath>
  </PropertyGroup>
</Project>
